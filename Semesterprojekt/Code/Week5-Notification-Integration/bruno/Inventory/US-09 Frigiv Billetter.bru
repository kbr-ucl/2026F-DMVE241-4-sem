meta {
  name: US-09 Frigiv Billetter
  type: http
  seq: 3
}

post {
  url: {{inventory_url}}/api/stock/release
  body: json
  auth: none
}

body:json {
  {
    "orderId": "{{orderId}}",
    "eventId": "{{eventId}}",
    "lines": [
      {
        "categoryId": "{{categoryId}}",
        "quantity": 2
      }
    ]
  }
}

tests {
  test("Status er 200 OK", function() {
    expect(res.status).to.equal(200);
  });
}
